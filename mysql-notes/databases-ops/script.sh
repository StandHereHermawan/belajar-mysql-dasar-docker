# 1. Command to List all Available databases in MySQL:
show databases;

# 2. Command create database in MySQL:
create database nama_database;

# 3. Command select created database in MySQL:
use nama_database;

# 4. Command remove created database in MySQL:
drop database nama_database;